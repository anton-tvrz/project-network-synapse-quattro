Workflow resilience polish: add retry policy to `store_backup` calls and guard post-success `update_device_status`/`log_audit_event` calls in the emergency-change permanent and TTL-revert paths so a status-backend hiccup after a successful config change is logged but does not fail the workflow. Drift and emergency workflow unit tests gain spy fixtures that assert on the activities called in failure paths instead of only the terminal exception message.
