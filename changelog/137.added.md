Backfill unit tests for the Infrahub seed-data population script (populate_sot.py) and the schema loading script (load_schemas.py), covering the GraphQL upsert helpers and all load/error paths with the API mocked.
