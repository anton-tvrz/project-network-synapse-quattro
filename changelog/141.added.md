Backfill unit tests for the Infrahub config client (lifecycle, auto-login, GraphQL execution, transforms), the generate_configs CLI (transform mode, error paths, exit codes), and BGP state validation in validate_state.py — bringing all three modules to 100% and backend coverage to 80%.
