Presentation block (NAF): a self-service FastAPI portal (`presentation/` workspace package) where authenticated users initiate deployments and operational overrides and list workflow executions. Per-user API keys with viewer/operator roles; the authenticated identity is server-set as the workflow operator and recorded in the Temporal memo for audit. Orchestrator-only integration per ADR-0005 — the portal never touches devices or the SoT directly.
